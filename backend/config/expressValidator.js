// To check entry form and avoid hacking
const {
    check,
    validationResult
} = require("express-validator");

module.exports = {
    check,
    validationResult
}

//export whereever you need to 